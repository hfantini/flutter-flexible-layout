name: flutter_flexible_layout
description: A flexible layout widget used to create responsive templates for mobile and web.
version: 1.0.0
homepage: henriquefantini.com

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: